package Applications;

import javafx.application.Application;

/** Punto de entrada compatible con la ejecución por classpath de IntelliJ. */
public class Inicio {
    public static void main(String[] args) {
        Application.launch(ViviendaApplication.class, args);
    }
}
